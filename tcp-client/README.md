#TCP client

## Description
This is a simple TCP client that can be used to test TCP servers. It will connect to the server at localhost:8888.

## Usage
```docker build -t word-of-wisdom-client .```

```docker run -it word-of-wisdom-client```